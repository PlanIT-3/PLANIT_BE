<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="woojooin.planit.domain.openAi.mapper.GoalProgressMapper">

    <!-- 멤버의 위험성향 조회 -->
    <select id="getRiskProfile" parameterType="Long" resultType="String">
        SELECT invest_type
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <!-- 멤버의 목표 달성률 조회 -->
    <select id="getIsaProgress" parameterType="map" resultType="double">
        SELECT
            isa_progress
        FROM daily_goal_progress
        WHERE goal_id = #{goalId}
        AND DAY(created_at) = 7
        ORDER BY created_at DESC
        LIMIT 6
    </select>

    <select id="getDepositProgress" parameterType="map" resultType="double">
        SELECT
            deposit_progress
        FROM daily_goal_progress
        WHERE goal_id = #{goalId}
        AND DAY(created_at) = 7
        ORDER BY created_at DESC
        LIMIT 6
    </select>


</mapper>